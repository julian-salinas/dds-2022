<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <script src="https://kit.fontawesome.com/64d58efce2.js"
            crossorigin="anonymous"></script>

    <link rel="stylesheet" href="forms.css" />

    <title>Login or Signup</title>
</head>
<body>
<div class="container">
    <div class="forms-container">
        <div class="signin-signup">
            <form action="/login" method="POST" class="sign-up-form">
                <h2 class="title">Login</h2>
               {{#if error}}
                   <p style="color:red;"> Error: {{descripcion}} </p>
               {{/if}}
                <div class="input-field">
                    <i class="fas fa-user"></i>
                    <input type="text" placeholder="Nombre de usuario" id="nombreInput" name="nombre"/>
                </div>
                <div class="input-field">
                    <i class="fas fa-lock"></i>
                    <input type="password" placeholder="Contraseña" id="passwordInput" name="password"/>
                </div>
                <input type="submit" value="Login" class="btn solid" />
            </form>
            <form action="/signin" method="POST" class="sign-in-form">
                <h2 class="title">Sign up</h2>
                    {{#if error}}
                        <p style="color:red;"> Error: {{descripcion}} </p>
                    {{/if}}
                <div class="input-field">
                    <i class="fas fa-user"></i>
                    <input type="text" placeholder="Nombre de usuario" id="nombreInput" name="nombre" />
                </div>
                <div class="input-field">
                    <i class="fas fa-lock"></i>
                    <input type="password" placeholder="Contraseña" id="passwordInput" name="password" />
                </div>

                <select class="select-field" id="tipo" name="tipo">
                    <option value="MIEMBRO" selected>MIEMBRO</option>
                    <option value="ORGANIZACION">ORGANIZACION</option>
                </select>

                <input type="submit" class="btn" value="Sign up" />
            </form>
        </div>
    </div>

    <div class="panels-container">
        <div class="panel left-panel">
            <div class="content">
                <h3>¿Ya tenés una cuenta?</h3>
                <p>
                    ¿Y que hacés acá? Logueate.
                </p>
                <button class="btn transparent" id="sign-up-btn">
                    Log In
                </button>
            </div>
            <img src="img/log.svg" class="image" alt="" />
        </div>
        <div class="panel right-panel">
            <div class="content">
                <h3>¿No tenés una cuenta?</h3>
                <p>
                    Registrate entonces, no te cuesta nada.
                </p>
                <button class="btn transparent" id="sign-in-btn">
                    Sign Up
                </button>
            </div>
            <img src="img/register.svg" class="image" alt="" />
        </div>
    </div>
</div>

<script>
    const sign_in_btn = document.querySelector("#sign-in-btn");
    const sign_up_btn = document.querySelector("#sign-up-btn");
    const container = document.querySelector(".container");

    sign_up_btn.addEventListener("click", () => {
        container.classList.add("sign-up-mode");
    });

    sign_in_btn.addEventListener("click", () => {
        container.classList.remove("sign-up-mode");
    });
</script>

</body>
</html>
